{
  "name": "samcards-landing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "hugeicons-vue": "^0.0.1",
    "vue": "^3.4.21"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.40.0",
    "@vitejs/plugin-vue": "^6.0.0",
    "vite": "^6.0.0",
    "wrangler": "^4.98.0"
  }
}
